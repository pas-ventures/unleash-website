# Venture Assessment — 2026-06-08 Update: production hardening, consent/privacy, security rotation, sender rename

Session summary of everything shipped and verified on 2026-06-08. The tool went from "silently losing submissions" to production-ready, honest, POPIA/GDPR-compliant, and fully secured.

## Shipped & verified live

**Frontend (GitHub Pages):**
- Honest, data-safe rebuild deployed — copy-box fallback, "email it to pas@" path, **never wipes the founder's work**, 3-min honest delivery cap (no fake "delivered").
- **POPIA/GDPR consent gate** — required tick on both send paths (review + confirm modals); fails *open* so a founder can never be locked out.
- **Wrong-recipient fix** — founder confirms the email their copy is sent to (prefilled, editable, validated) instead of the worker regex-guessing the first email in the chat. Closes a confidentiality-leak risk.
- **Privacy modal rewritten** to be accurate (it previously claimed "we don't store on Unleash servers" — false once KV+Notion went live).
- **30-day** browser-draft retention (was 14).
- New standalone **/privacy** policy page (POPIA + GDPR) + **Privacy Policy** link added to the homepage footer.

**Worker (Cloudflare, auto-deploy via GitHub Action now wired):**
- Email **sender display name** changed from `Luna` → **`Unleash Ventures`** (address unchanged, verified Resend domain).
- Worker + wrangler config committed to git (resolved prior drift between repo and live).

**Security — fully rotated & closed:**
- Anthropic + Resend API keys (exposed in earlier chat) **rotated**, old keys deleted, both verified working end-to-end.
- All old/exposed Cloudflare API tokens revoked; fresh scoped `unleash-deploy` token lives only in GitHub Actions secrets.
- GitHub Actions secrets updated to the new keys.

## Verification (evidence, not claims)
- End-to-end `/api/done` tests: submission persisted to KV + Notion, **email landed in inbox (not spam)** to Google Workspace + consumer Gmail.
- Adversarial pass: foreign origin → 403, no origin → 403, empty input → 400, unknown path / bad method → 404/403, HTML escaped in email. **Could not break it.**
- Post-rotation re-test: Anthropic (chat + summary) + Resend (send) confirmed on the new keys.

## Open backlog → ClickUp (under 869d50ppu "CLAUDE LUNA IMPROVEMENTS BACKLOG")
- **869dkquzk** — Membership platform (sign-up → approve → log-in + per-user saved assessments) — *high*
- **869dkquzy** — Resend webhook for honest "Delivered ✓" badge — *normal*
- **869dkqv1u** — Cleanup (delete test rows/emails, confirm Gmail spelling, optional de-Luna body, drop unused Onboarding keys, refresh Notion log) — *low*
- **869dkqv6g** — Phase 2 polish (streaming, token-cap summarisation, Turnstile CAPTCHA, accessibility, PDF export) — *low*

## Verdict
Production-ready. Safe to onboard Jamie and the Q3 cohort. Nothing broken.
